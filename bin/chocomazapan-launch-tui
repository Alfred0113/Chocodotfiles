#!/bin/bash

# meta:summary=Launch a TUI command in the default terminal with ChocoMazapan styling
# meta:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.chocomazapan.$(basename $1) -e "$1" "${@:2}"
