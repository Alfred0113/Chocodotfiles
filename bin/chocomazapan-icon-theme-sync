#!/usr/bin/env bash
# Compat: el tema de iconos ahora lo maneja chocomazapan-gtk-theme-sync junto con
# el tema GTK de Yaru. Este wrapper se mantiene por si algo viejo aun lo llama.
exec "$(dirname "$0")/chocomazapan-gtk-theme-sync" "$@"
