#!/bin/bash

# meta:summary=Lock the computer and turn off the display
# meta:group=system
# meta:name=lock
# meta:examples=chocomazapan-system-lock

if ! pidof hyprlock >/dev/null; then
  (
    hyprlock
    chocomazapan-system-wake
  ) &
fi

# Set keyboard layout to default (first layout)
hyprctl switchxkblayout all 0 > /dev/null 2>&1

# Ensure 1password is locked
if pgrep -x "1password" >/dev/null; then
  1password --lock &
fi

# Avoid running screensaver when locked
pkill -f org.omarchy.screensaver

if [[ ${CHOCOMAZAPAN_LOCK_ONLY:-false} != "true" ]]; then
  (
    sleep 10
    pidof hyprlock >/dev/null || exit 0
    omarchy-brightness-keyboard off
    omarchy-brightness-display off
  ) &
fi
