# Hereda todas las secuencias estándar del locale (teclas muertas: ´ + a = á, etc.)
include "%L"

# Atajos personales — archivo local, NO versionado. Pon tus propias líneas
# <Multi_key> ... : "texto" en ~/.XCompose.local (install.sh lo crea vacío).
include "%H/.XCompose.local"
